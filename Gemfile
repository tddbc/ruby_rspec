source 'https://rubygems.org'

group :development do
  gem 'rspec', '2.13.0'

  gem 'guard-rspec'

  # Runs on Mac OS X
  gem 'growl'
  # Runs on Linux, FreeBSD, OpenBSD and Solaris
  gem 'libnotify'
  # Runs on Windows
  gem 'rb-notifu'
end
